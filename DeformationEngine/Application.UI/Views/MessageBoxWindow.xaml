<Window
    x:Class="Application.UI.Views.MessageBoxWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Style="{StaticResource AppWindowStyle}"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner"
    MinWidth="350"
    MinHeight="160">
    <Border Background="{StaticResource BackgroundBrush}" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{StaticResource PanelBrush}">
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" 
                           VerticalAlignment="Center" 
                           Margin="10,0,0,0" 
                           FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right" 
                            WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Content="✕" Style="{StaticResource CaptionButtonStyle}" Click="Close_OnClick"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="20,20,20,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           x:Name="MessageText" 
                           TextWrapping="Wrap" 
                           MaxWidth="400" 
                           Margin="0,0,0,20"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Row="1" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button x:Name="OkButton" Content="OK" Width="80" Click="Ok_OnClick" Visibility="Collapsed" Margin="5,0,0,0"/>
                    <Button x:Name="YesButton" Content="Yes" Width="80" Click="Yes_OnClick" Visibility="Collapsed" Margin="5,0,0,0"/>
                    <Button x:Name="NoButton" Content="No" Width="80" Click="No_OnClick" Visibility="Collapsed" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>